export default function create_check(default_value, unit) {
  if (capture_categorical_options) {
// get all values from array and return a function that checks if value is accepted
} else {
    if (unit == "perc") {
        // return funciton that checks if value 0=<val=<100
    }
  }
//   return function that checks if value is numeric 
  ;
}
